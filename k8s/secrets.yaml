# ═══════════════════════════════════════════════════════
#  Kubernetes — Secrets (DO NOT commit real values!)
#  Usage: kubectl apply -f k8s/secrets.yaml
# ═══════════════════════════════════════════════════════
apiVersion: v1
kind: Secret
metadata:
  name: gta5-secrets
  namespace: gta5-game
type: Opaque
stringData:
  # Replace with real values before applying
  redis-url: "redis://gta5-redis:6379"
  jwt-secret: "CHANGE_ME_STRONG_SECRET_KEY_HERE"
